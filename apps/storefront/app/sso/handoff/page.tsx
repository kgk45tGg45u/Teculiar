"use client";

import { SsoHandoffScreen } from "@teculiar/web-core/components/auth/sso-handoff-screen";

// SSO handoff, storefront side (Phase 4.6e/2.4) — see SsoHandoffScreen for the flow.
export default function SsoHandoffPage() {
  return <SsoHandoffScreen />;
}
